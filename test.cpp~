#include <time.h>
#include <stdio.h>

int main(void)
{
   clock_t start, end;
   long l;

   start = clock();

   for (;;){

   	end = clock();
   	printf("%f秒\n", (double)(end - start) / CLOCKS_PER_SEC/10);
   }
   return 0;
}
